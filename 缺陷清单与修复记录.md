# 缺陷清单与修复记录（2026-07-22）

> 范围：本会话围绕"问卷提交返回 400"展开的排查，以及由此牵引出的启动/状态相关问题。
> 判断标准：🔴 真实代码缺陷（需修）｜🟡 体验瑕疵（建议修）｜⚪ 环境/配置（非代码，绕过即可）

---

## 一、本次确认并修复的缺陷

### 🟡 BUG-01｜问卷提交后前端未刷新本地状态，可重复进表单撞 400
- **位置**：`frontend/src/pages/Questionnaire.tsx`
- **现象**：提交问卷成功后只 `navigate('/complete')`，未刷新 Zustand store 里的 `user.status`。用户按浏览器"后退"回到问卷页时，前端路由守卫（`App.tsx` 用 store 的 `user.status`）仍认为处于 `task_completed`，于是再次放行进表单；二次点击提交时后端状态已是 `questionnaire_completed`，触发 `questionnaire.py` 的防重复提交守卫 `if user.status != TASK_COMPLETED: 400`，返回"请先完成任务"。
- **根因**：前端状态与后端状态机不同步（store 未随提交流程更新）。
- **修复**（已落地）：
  1. 提交成功后调用 `await useUserStore.getState().fetchMe()` 拉取最新状态；
  2. 新增 `submitted` 本地状态，提交后按钮置为"已提交"并禁用，防重复点击。
- **验证**：`npm run build` 通过（tsc -b && vite build，0 错误）。

---

## 二、排查"400"时确认"非 bug"的点（避免误修）

| 项 | 结论 |
|---|---|
| 邮件无 Key 是否阻断任务 | 否。`email_service.py` 在无 `RESEND_API_KEY` 时返回 `mock_sent`，前端视为成功，`emailSent=true`，四项子任务可正常集齐并提交任务。 |
| 任务提交是否卡死 | 否。DB 状态分布验证：无账号卡在 `task_in_progress`/`task_completed`。 |
| 400 是否代码崩溃 | 否。是后端**防重复提交守卫**正常生效：账号已是 `questionnaire_completed`，二次提交被拦。属实验"一人一号、只跑一次"的设计保护。 |

---

## 三、环境/配置类（非代码 bug，已通过文档规避）

| 项 | 现象 | 规避方式 |
|---|---|---|
| 🔴（历史上已修）JWT 分组刷新 | 早期 `/consent` 未回传含 group 的新 token | 已在 `auth.py` 修复并返回新 token |
| ⚪ `activate.ps1` 被 PowerShell 拦 | 直接 `.\venv\Scripts\activate` 报 PSSecurityException | 改用 `.\venv\Scripts\python.exe -m uvicorn ...` |
| ⚪ `npm`/`npm.ps1` 被 PowerShell 拦 | `npm` 解析到被拦的 `.ps1` | 改用 `npm.cmd run dev`（或 `Set-ExecutionPolicy -Scope Process Bypass`） |
| ⚪ psql 保留字 `"group"` 在 `-c` 丢引号 | 直接 `-c` 传参会丢引号导致语法错 | 用 `.sql` 文件 + `psql -f` 执行最稳 |
| ⚪ psql 中文 NOTICE 编码错（GBK vs UTF8） | 含中文的 SQL 文件被按 UTF8 读报编码错 | SQL 文件内避免中文，或设 `PGCLIENTENCODING=UTF8` |
| ⚪ `NODE_OPTIONS=--use-system-ca` 被 node 拒 | 该 node 版本不允许此选项 | 构建前 `.$env:NODE_OPTIONS=""` |

---

## 四、当前状态

- `test01@test.com` 已重置为 `registered`（group 清空、问卷答案与任务提交记录已删除），可重新走完整流程。
- `Questionnaire.tsx` 已修正并构建通过。
- 其余账号均为已完成或仅到同意书状态，无卡死。

## 五、后续可选优化（未做，待确认）
1. 提交问卷成功后增加显式"已完成"提示页/弹窗，降低用户误以为失败而重复提交的概率。
2. 后端 `questionnaire/submit` 的 400 文案可更友好（如"您已完成问卷，无需重复提交"），便于区分"未完成任务"与"已提交"。
