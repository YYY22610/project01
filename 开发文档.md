# 人机协作决策实验平台 — 网站开发文档

> 文档版本：v1.0 ｜ 编写日期：2026-07-26 ｜ 状态：开发中（可支撑对照实验开展）
> 需求来源：《研究二：人机协作决策实验平台 — 功能需求文档》（`7_21实验网站设计.docx`）
> 代码基线：`travel-experiment-platform/`（截至 2026-07-24）

---

## 1. 文档信息

| 项 | 内容 |
|----|------|
| 项目名称 | 人机协作决策实验平台（Human–AI Collaboration Decision Experiment Platform） |
| 文档类型 | 网站开发文档（需求 + 设计 + 实现 + 符合性对照） |
| 技术负责人 | 苏樱紫 |
| 技术栈 | React 18 + TypeScript + Vite + Tailwind + Zustand ／ FastAPI + SQLAlchemy 2.0(async) + PostgreSQL 17 ／ SSE ／ python-docx ／ LLM Function Calling |
| 适用范围 | 参与者端（H / H+SOA / H+MOA 三组）、管理后台、后端服务、数据库 |

---

## 2. 项目概述

### 2.1 背景与目标
平台用于开展"智能体助理决策效能"的**三组间对照实验**，比较人类单独决策与不同数量 AI 助理介入下的决策质量差异，并采集决策过程中的主客观数据，填补"意图—行为"差距的实证空白。

### 2.2 实验设计
- **三组间对照**：H 组（人类单独决策）、H+SOA 组（人 + 单一 AI 助理）、H+MOA 组（人 + 多个 AI 助理，本实现为 3 个：信息检索 / 行程编排 / 事务处理）。
- **单盲设计**：分组信息对参与者不可见，前端不展示"您被分到 X 组"。
- **核心任务**：综合性旅行规划 —— 信息检索、Word 文档生成、日程提醒设置、跨平台邮件发送。
- **数据采集**：客观绩效指标 + 行为日志 + 任务后主观评价。
- **评分**：八因子百分制，依据 GB/T 18972-2017《旅游资源分类、调查与评价》。

### 2.3 术语
| 术语 | 说明 |
|------|------|
| SOA | Single AI Assistant，单 AI 助理组 |
| MOA | Multi AI Assistants，多 AI 助理组 |
| SSE | Server-Sent Events，服务端单向流式推送 |
| OpenClaw | 需求文档中指定的 AI 助理形态（本实现以自研 LLM Agent 等价替代，见 §9 偏差 D1） |

---

## 3. 需求规格（摘录自需求文档）

| 编号 | 需求 | 关键约束 |
|------|------|----------|
| M1 | 用户注册与随机分组 | 邮箱注册；采集人口统计；1:1:1 自动分组；单盲；管理员可微调 |
| M2 | 流程状态机 | 注册→知情→演示→任务中→提交→问卷→完成；逐级解锁不可跳转 |
| M3 | 任务说明与演示 | 三组相同任务指令；分组差异化演示；确认机制 + 观看时长 |
| M4 | 任务执行界面（H/SOA/MOA） | 固定任务面板 + 计时器 + 四项子任务 Checklist；SOA 对话窗口；MOA 多助理协同 |
| M5 | 任务后问卷 | 信任/自主性/满意度/任务负荷/使用意愿/操纵检验 6 构念；H 组改写 |
| M6 | 数据采集与评分 | 子任务校验、提醒正确性、邮件成功率、用时、行为日志；八因子评分录入 |
| M7 | 管理后台 | 监控面板、OpenClaw 状态、基础配置、数据导出 |
| M8 | 关键注意项 | 分组不可见、任务标准化、失败容错、草稿自动保存、知情同意 |

---

## 4. 系统总体设计

### 4.1 技术架构
| 层 | 技术 |
|----|------|
| 前端 | React 18 · TypeScript · Vite · Tailwind CSS · Zustand · React Router v6 |
| 后端 | Python · FastAPI · SQLAlchemy 2.0（异步）· asyncpg |
| 数据库 | PostgreSQL 17（10 张表） |
| 实时通信 | SSE（Agent 流式回复与执行状态推送，通道已搭建，真实 AI 流式待接入） |
| AI Agent | LLM API + Function Calling；`MockLLMClient` / `RealLLMClient` 工厂 |
| 文档生成 | `python-docx` 将 HTML 富文本转为带格式 `.docx` |
| 搜索 | `httpx` + 标准库 `html.parser` 爬取 DuckDuckGo / Bing HTML（免 API Key） |

### 4.2 分层架构（请求流）
```
参与者浏览器 (React SPA)
      │  REST（请求/响应）
      ▼
FastAPI 网关 (auth / task / search / document / email / agent / log / questionnaire / admin)
      │
      ├─ 业务服务层 (auth / group / document / email / search / state_machine)
      ├─ Agent 引擎 (BaseAgent + 工具集 search/generate_docx/budget/reminder/email)
      └─ 数据访问层 (SQLAlchemy ORM → PostgreSQL 17)
      ▲
      │  SSE（Agent 流式回复 / 工具调用状态）
参与者浏览器
```

### 4.3 核心数据流
1. 注册 → `assign_group()` 区块随机化写入 `users.group` → 下发 JWT（含 `group` 声明，仅用于前端判断"是否显示 AI 面板"，**不向用户展示组名**）。
2. 任务执行 → 前端 `useBehaviorLogger` 批量上报操作事件 → `behavior_logs`。
3. 提交 → 校验四项子任务 → `task_submissions`；生成/下载 Word → `uploads/documents/`。
4. 评分 → 管理员在后台录入八因子 → `admin_scores.compute_total()` 汇总 0–100。
5. 导出 → 后台 `/export/*` 生成 CSV / XLSX。

### 4.4 关键设计决策
- **单盲保证**：参与者 UI 不渲染组名；MOA/SOA 的 AI 面板显隐由 JWT `group` 驱动，用户无感知。
- **草稿自动保存**：任务进度写入 `localStorage`，刷新/断网不丢（需求 M8）。
- **Mock 优先**：LLM 默认 Mock 模式，便于无密钥联调；配置 `LLM_API_KEY` 切换真实模型。因此目前 AI 助理实际未接入真实大模型，对话与流式回复均为 Mock，真实决策辅助能力待接入后具备。
- **SSE 而非 WebSocket**：SSE 通信通道代码已搭建（用于 Agent 流式回复与工具调用状态推送），但真实 AI 流式回复尚未接入与实测，当前为 Mock 行为（见 §9 偏差 D2）。

---

## 5. 数据库设计

### 5.1 数据表清单（共 10 张）
`users` · `behavior_logs` · `task_submissions` · `chat_messages` · `questionnaire_items` · `questionnaire_responses` · `admin_scores` · `reminders` · `system_config` · `admin_users`

### 5.2 核心表结构
**users**
| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID PK | 用户唯一 ID |
| email / password_hash | str | 登录凭据 |
| group | enum(H/SOA/MOA) | 分组（单盲，前端不展示） |
| status | enum | 状态机当前状态 |
| age/gender/education/tech_frequency/ai_experience | str? | 人口统计（采集时机见偏差 D4） |
| demo_watch_seconds | int? | 演示观看时长 |
| task_start_time / task_end_time | datetime? | 计时效用 |

**behavior_logs**（行为日志，字段对照需求"七"）
| 字段 | 说明 |
|------|------|
| action_type | 16 类操作（search_query / document_edit / agent_message / task_submit …） |
| group / session_id / user_agent / screen_resolution / experiment_version | 会话元数据 |
| input_content / ai_response / agent_id / user_action_on_ai | AI 交互行为 |
| results_viewed / result_view_duration_ms / clicked_item_id | 信息检索行为 |
| request_latency_ms / is_success / error_detail | 性能与结果 |
| phase / manual_edit_count / final_plan_submit_time | 决策过程指标 |

**task_submissions**：`task1_search~task4_email`（布尔）、`docx_file_path`、`reminder_datetime`、`email_status`、`duration_ms`。
**admin_scores**：八因子 `scenic/historic/rarity/scale/integrity/fame/season`（各 0–10）+ `eco_score`(-5~+3) + `total_score`(0–100，clamp) + `notes`。
**questionnaire_items / _responses**：6 构念（trust/autonomy/satisfaction/task_load/future_use/manipulation_check），题项含 `applicable_groups`（按组差异化展示）。
**reminders / system_config / admin_users / chat_messages**：提醒、实验参数键值、管理员、对话历史。

---

## 6. 模块设计与实现

### 模块 1 — 用户注册与随机分组
- **实现**：`routers/auth.py` + `services/group_service.py` + `services/state_machine.py`。
- **随机分组**：区块随机化，按 `target_sample_size/3` 计算每组配额，优先补最少人数组；管理员后台可手动调整。
- **状态机**：`REGISTERED→CONSENTED→DEMO_COMPLETED→TASK_IN_PROGRESS→TASK_COMPLETED→QUESTIONNAIRE_COMPLETED`（较需求多"知情同意"节点，满足实验伦理 M8）。
- **单盲**：前端从 JWT 读取 `group` 仅用于显隐 AI 面板，文本中不出现组名。

### 模块 2 — 任务说明与演示
- **统一指令**：任务参数（目的地 / 天数 N / 预算 M / 目标邮箱）来自 `system_config`，三组一致。
- **差异化演示**：`pages/Demo.tsx` 按组展示不同引导内容；SOA 展示助理能力说明，MOA 展示多助理分工。
- **确认机制**：须点击"我已理解任务要求"方可进入任务；`demo_watch_seconds` 记录时长。

### 模块 3 — 任务执行界面（核心）
- **通用组件**：固定任务面板（四项子任务 Checklist）、计时器（`useTimer`）、提交按钮（四项全完成方可提交）。
- **H 组**：搜索引擎（DuckDuckGo/Bing 爬虫）、富文本编辑器、日历提醒、邮件发送——无 AI 助理。
- **SOA 组**：左侧 `ChatWindow` 对话窗口，SSE 通道 + 执行步骤可视化（`aiSteps`）+ 结果预览/编辑 + 可中断重下指令（真实 AI 回复当前为 Mock，待接入大模型）。
- **MOA 组**：三个独立助理面板 `MOAAgentPanel`（信息检索 / 行程编排 / 事务处理），分工标题可视化，每个助理独立日志流；通过分面板分别下达指令（等价于 @路由）。

### 模块 4 — 任务后问卷
- **实现**：`pages/Questionnaire.tsx` + `models/questionnaire.py`。
- 6 构念题项由管理员后台动态配置（`questionnaire_items`），`applicable_groups` 控制 H 组改写为"工具"评价、SOA/MOA 保留 AI 表述。
- 题型支持 Likert（5/7 级）、单选、文本。

### 模块 5 — 数据采集与评分
- **客观指标**：子任务逐项校验、提醒正确性、邮件发送状态、任务总用时、全量行为日志（JSON 上报）。
- **八因子评分**：`models/admin_score.py` 实现 `FACTOR_WEIGHTS`（30/25/15/10/5/10/5，正权合计 100）+ 环保调整(-5~+3)，`compute_total()` 汇总并 clamp 至 0–100，依据 GB/T 18972-2017。
- **数据导出**：`routers/admin.py` 提供 `/export/participants|logs|scores|all`（CSV）与 `/export/all/xlsx`（Excel）。

### 模块 6 — 管理后台
- **实现**：`pages/admin/`（8 个页面）。
- 监控面板：各组实时样本量、总注册/进行中/已完成、异常报警（用时超 3σ）。
- OpenClaw 状态：`/openclaw/status` 展示调用成功率/平均响应时间；`/openclaw/toggle` 支持实验中止开关。
- 基础配置：动态调整 N / M / 收件邮箱、启用停用实验、导出原始数据。

---

## 7. 接口规范（API 一览）

| 模块 | 端点 | 说明 |
|------|------|------|
| 认证 | `POST /api/auth/register` · `/login` · `/consent` | 注册 / 登录 / 知情同意 |
| 任务 | `GET /api/task/config` · `/status` · `POST /task/start` · `/submit` | 任务配置与提交 |
| 搜索 | `POST /api/search` | 景点检索（DuckDuckGo+Bing，返回 `source` 标记实时/示例） |
| 文档 | `POST /api/document/generate` · `GET /api/document/download/{file}` | Word 生成与下载 |
| 提醒 | `POST /api/reminder` | 设置提醒 |
| 邮件 | `POST /api/email/send` | 发送行程邮件 |
| Agent | `POST /api/agent/chat`（SSE） | AI 助理对话通道（soa / moa_a / moa_b / moa_c），真实模型待接入 |
| 行为日志 | `POST /api/log/batch` | 批量上报操作日志 |
| 问卷 | `GET/POST /api/questionnaire/*` | 题项获取与作答 |
| 管理后台 | `/api/admin/dashboard` · `/logs` · `/scores/{id}` · `/openclaw/*` · `/export/*` | 监控 / 日志 / 评分 / 状态 / 导出 |

---

## 8. 关键实现说明

### 8.1 搜索引擎（`services/search_service.py`）
后端 `httpx` 抓取 **DuckDuckGo HTML** 结果页，自写 `_ResultExtractor` 状态机解析 `title/url/snippet`；异常时切换 **Bing** 兜底；外网不可达回退本地 10 条杭州示例（前端标"示例数据"）。**零 API Key**。前端搜索子任务提供"搜索引擎 / AI 推荐"双 Tab。

### 8.2 文档生成（`services/document_service.py`）
前端 `contenteditable` 富文本编辑器（加粗/斜体/下划线、H1/H2、有序/无序列表、插表格、清格式），提交 HTML 至后端；`_HtmlToDocx` 用 `html.parser` 转为带格式 `.docx`（标题层级/加粗/列表/表格均保留）。另支持浏览器端"保存到本地"直接导出 `.doc`。

### 8.3 AI Agent 框架（`services/agents/`）— 框架代码已搭，真实模型待接入
> 状态说明：以下为已实现的代码框架；真实大模型尚未接入（默认 Mock），Function Calling 与流式输出目前仅在 Mock 下验证，真实决策辅助能力待配置 `LLM_API_KEY` 后具备。
- `BaseAgent`：`system_prompt + tools` 驱动 LLM 对话循环的框架，预留 Function Calling（tool_call → execute → 二次 LLM → 流式输出）与 SSE 推送接口。
- `agent_factory`：配置 soa / moa_a / moa_b / moa_c 四套提示词与工具集。
- `llm_client`：`MockLLMClient`（关键词模拟）/ `RealLLMClient`（OpenAI 兼容 API）工厂，按 `LLM_API_KEY` 切换。
- `tools/agent_tools`：5 个工具（search_attractions / generate_docx / calculate_budget / set_reminder / send_email），其中 `search_attractions` 当前返回 Mock 数据（见偏差 D3）。

### 8.4 行为日志（`hooks/useBehaviorLogger.ts`）
模块级队列 + 5s 定时批量上报；失败落入 `localStorage` 待重发；每次上报自动附带会话元数据（user_agent / 分辨率 / 版本 / 开始时间）。覆盖需求"七"全部字段类别。

### 8.5 八因子评分（`models/admin_score.py`）
权重严格对应 GB/T 18972-2017：观赏游憩 30 / 历史文化 25 / 珍稀奇特 15 / 规模 10 / 完整性 5 / 知名度 10 / 适游期 5（合计 100），环保附加值 -5~+3，`compute_total()` 加权汇总并 clamp。

---

## 9. 需求符合性对照表（设计文档 → 实现状态）

图例：✅ 已实现 ｜ 🟡 部分实现 ｜ ⚠️ 偏差（已实现但技术路线/细节不同） ｜ ⬜ 未实现

| 需求项 | 状态 | 说明 |
|--------|------|------|
| M1.1 邮箱注册 + 人口统计 | ⚠️ | 注册已实现（邮箱+密码）；人口统计字段已建，采集改独立步骤（偏差 D4） |
| M1.2 1:1:1 随机分组 + 单盲 | ✅ | 区块随机化；前端不展示组名 |
| M1.3 状态机逐级解锁 | ✅ | 含知情同意节点（比需求多一步） |
| M2.1 统一任务指令 | ✅ | 参数来自 system_config，三组一致 |
| M2.2 分组差异化演示 | 🟡 | 演示页存在，MOA 演示内容为图文简化，非动画 |
| M2.3 演示确认 + 时长记录 | ✅ | `demo_watch_seconds` |
| M3.1 通用组件（面板/计时器/提交） | ✅ | |
| M3.2 H 组界面 | ✅ | 真实爬虫搜索 + 编辑器 + 日历 + 邮箱 |
| M3.3 SOA 界面 | ✅ | 对话窗口 + 执行步骤可视化 + 预览编辑 + 干预（真实 AI 流式回复待接入，当前 Mock） |
| M3.4 MOA 界面 | ✅ | 3 独立助理面板 + 分工可视化（偏差 D1/D2：SSE 而非 WebSocket，未用 OpenClaw；真实 AI 待接入） |
| M4 任务后问卷（6 构念） | ✅ | 动态题项 + 按组适用 |
| M5.1 客观指标采集 | ✅ | 子任务/提醒/邮件/用时/行为日志 |
| M5.2 八因子评分录入 | ✅ | 权重与 GB/T 18972-2017 对齐 |
| M5.3 数据导出 CSV/Excel | ✅ | CSV + XLSX，按组筛选 |
| M6.1 监控面板 | ✅ | 样本量/进度/异常报警 |
| M6.2 OpenClaw 状态监控 | 🟡 | status/toggle 已实现；仅真实 LLM 下统计有意义，Mock 下为空 |
| M6.3 基础配置 | ✅ | N/M/邮箱/启停/导出 |
| M8 关键注意项 | ✅ | 单盲/标准化/失败容错/草稿保存/知情同意 均已覆盖 |

### 主要偏差汇总
- **D1（AI 助理形态）**：需求指定对接 OpenClaw（Shell/文件/邮件能力）；实现采用自研 LLM Agent 框架（OpenAI 兼容 + Function Calling 工具集），默认 Mock。功能等价，技术路线不同。
- **D2（实时通信）**：需求指定 WebSocket；实现采用 SSE，满足流式推送但非 WebSocket。
- **D3（Agent 搜索工具）**：直接搜索端点用真实爬虫；Agent 的 `search_attractions` 工具当前返回 Mock 数据，未复用真实爬虫。
- **D4（人口统计采集时机）**：由注册时采集改为独立采集步骤（字段已在库）。
- **D5（预算/天数默认值）**：system_config 可配；SOA 提示词硬编码 1000 元 / 3 天，建议改为读取配置。

---

## 10. 已知问题与技术债务
1. Agent `search_attractions` 未接入真实爬虫（D3），MOA/H+SOA 下 AI 搜景点返回示例数据。
2. 真实 LLM 未接入（默认 Mock），需配置 `LLM_API_KEY` / `LLM_API_BASE_URL` / `LLM_MODEL` 后方具备真实决策辅助能力。
3. MOA 为分面板独立对话，缺"向全体广播指令"入口（需求 3.4 协调控制）。
4. `admin_score` 保留旧字段 `rationality_score/quality_score`，新八因子为权威字段，旧字段待清理或迁移。
5. 演示页 MOA 内容偏简化，建议补充多助理协同示意。
6. OpenClaw 状态监控在 Mock 模式下无真实调用统计，真实部署后需验证。

---

## 11. 部署与运行
1. **数据库**：PostgreSQL 17；导入 `database/schema.sql` + `seed.sql`（含系统配置、问卷题项）。
2. **后端**：`backend/venv` 已预置；`uvicorn app.main:app --port 8000 --reload`。
3. **前端**：`frontend/`；`npm install` → `npm run dev`（默认 5173）。
4. **顺序**：数据库 → 后端 → 前端（三者缺一不可；前端代理指向 `127.0.0.1:8000`）。
5. **环境变量**：`DATABASE_URL` / `SECRET_KEY` / `LLM_API_KEY`（留空=Mock）/ `SENDER_EMAIL` / `BING_SEARCH_API_KEY`（可选兜底）等，见 `.env.example`。
6. **部署**：前端 `npm run build` 静态托管；后端 Gunicorn+Uvicorn；数据库 Docker 或云托管。

---

## 12. 后续工作计划
- [ ] 接入真实 LLM（RealLLMClient）并完善 Agent 工具链。
- [ ] 修复 D3：Agent 搜索工具复用真实爬虫。
- [ ] MOA 增加"全体广播"指令入口与跨助理协调视图。
- [ ] 开展小样本预实验，校准八因子评分一致性（ICC）。
- [ ] 行为数据可视化分析与一键导出看板。
- [ ] 清理 `admin_score` 旧字段，统一八因子为权威评分。

---
*本文档依据需求文档 `7_21实验网站设计.docx` 与 `travel-experiment-platform/` 当前代码实现整理，覆盖需求、设计、实现与符合性对照，可作为开发基线与人机协作实验开展的工程依据。*
