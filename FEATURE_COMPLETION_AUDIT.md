# 功能完成度与跑通验证报告

> 审计对象：`travel-experiment-platform`（人机协作决策实验平台）
> 对照文档：`D:/27475/27475/功能需求文档(1).docx`
> 审计日期：2026-07-20
> 方法：需求文档提取 → 代码逐模块核对 → 启动 PostgreSQL/后端/前端 → 端到端接口测试 → 修复崩溃并复测

---

## 一、完成度矩阵（对照需求 6 大模块）

### 模块 1：用户注册与随机分组系统
| 需求项 | 状态 | 代码位置 | 说明 |
|--------|------|----------|------|
| 1.1 注册/登录 + 人口统计学信息 | ✅ 已完成 | `auth.py`(register/login/consent/demographics)、`Consent.tsx`(采集 age/gender/education/tech_frequency/ai_experience)、`models/user.py` | 用户模型含全部人口统计字段，Consent 页有表单 |
| 1.2 自动随机分组 1:1:1 | ✅ 已完成 | `group_service.py`(block randomization，容量校验) | 单盲：`/auth/me` 不含 group 字段 |
| 1.2 管理员手动微调分组 | ❌ 未实现 | `admin.py` 无改分组端点 | 需求要求"支持手动微调"，当前只能查看 |
| 1.3 流程状态机 | ✅ 已完成 | `state_machine.py` | ⚠️ 缺最终"已完成"终态（止于 questionnaire_completed） |

### 模块 2：任务说明与演示模块
| 需求项 | 状态 | 代码位置 | 说明 |
|--------|------|----------|------|
| 2.1 统一任务指令展示 | ✅ 已完成 | `Demo.tsx`、`task.py`(config) | 三组看到相同任务指令 + 4 子任务 |
| 2.2 分组差异化演示 | ❌ 未实现 | `Demo.tsx` | 三组显示完全相同内容，无 H/SOA/MOA 差异化演示 |
| 2.3 演示确认机制 + 观看时长 | ❌ 未实现 | `Demo.tsx` | 无"我已理解任务要求"按钮，无演示时长记录 |

### 模块 3：任务执行界面（核心）
| 需求项 | 状态 | 代码位置 | 说明 |
|--------|------|----------|------|
| 3.1 通用组件（清单/计时器/提交） | ✅ 已完成 | `Task.tsx`、`useTimer.ts` | checklist、计时器、提交按钮齐备 |
| 3.2 H 组界面（搜索/文档/日历/邮箱） | ✅ 已完成 | `Task.tsx` + `search/document/reminder/email.py` | 四子任务全部可操作 |
| 3.3 H+SOA（对话/执行可视化/预览编辑） | ⚠️ 结构完成，前端有阻断 bug | `agent.py`(SSE)、`ChatWindow`、`ExecutionLog`、`useAgentChat.ts` | 后端可用（Mock LLM 无需密钥）；**前端 AI 面板对首次会话用户不可见**（见第三节 bug 3） |
| 3.4 H+MOA（多助理/分工/独立日志/协调） | ⚠️ 同上 | `Task.tsx`(3×`MOAAgentPanel`)、`agent.py`(moa_a/b/c) | 结构完成；同前端阻断 bug；WebSocket 需求以 SSE 实现（功能等价） |

### 模块 4：任务后评价问卷
| 需求项 | 状态 | 代码位置 | 说明 |
|--------|------|----------|------|
| 问卷采集（前端+后端+15 题） | ✅ 已完成 | `Questionnaire.tsx`、`questionnaire.py`、种子 15 题 | 可正常获取题项并提交 |
| 构念与需求一致 | ❌ 不符 | 种子数据 | 实际构念：PU/PEOU/trust/SAT/CI/workload（TAM 体系）；需求要求：信任/感知自主性/满意度/任务负荷/未来使用意愿/操纵检验。**缺失"感知自主性""操纵检验"；命名不一致** |

### 模块 5：数据采集与客观评分系统
| 需求项 | 状态 | 代码位置 | 说明 |
|--------|------|----------|------|
| 5.1 客观指标采集 | ✅ 已完成 | `log.py`+`useBehaviorLogger`、`task_submission`、`email_status`、`duration_ms` | ⚠️ **H3：邮件发送失败被记为 `mock_sent`（静默成功），数据真实性风险** |
| 5.2 评分录入界面 | ✅ 已完成 | `admin.py`(`/scores/{user_id}`) | 行程合理性 + 综合质量评分 |
| 5.3 数据导出 | ✅ 已完成（CSV） | `admin.py`(`/export/participants|logs|scores`) | ⚠️ 仅 CSV，需求要求 CSV/Excel |

### 模块 6：管理后台
| 需求项 | 状态 | 代码位置 | 说明 |
|--------|------|----------|------|
| 6.1 实验监控面板 | ✅ 已完成 | `admin.py`(`/dashboard`) | 各组样本量/进度/均分（修复后跑通） |
| 6.1 异常报警（3×标准差） | ❌ 未实现 | — | 需求要求标记异常用户，未实现 |
| 6.2 OpenClaw 服务状态监控 | ❌ 未实现 | — | 无 API 成功率/响应时间监控端点或 UI |
| 6.3 基础配置 | ✅ 已完成 | `admin.py`(`/config` get/put) | 任务参数、启用停用可配置 |

---

## 二、跑通验证结果（端到端）

**环境**：Windows / PostgreSQL 17（`D:\Develop\PostgreSQL`）/ 后端 uvicorn:8000 / 前端 Vite:5173
**测试覆盖**：注册→同意→演示→开始任务→4 子任务→提交→问卷→管理后台全链路（自动化脚本 urllib 直连 + 代理）

**修复前 e2e：13 / 19 通过 → 修复后 16 / 19 通过**

| 验证项 | 结果 | 备注 |
|--------|------|------|
| 注册 / 登录 / 单盲(/me 无 group) | ✅ | |
| 同意并分配分组（DB 确认 H/SOA/MOA 落库） | ✅ | |
| 完成演示 / 开始任务 | ✅ | |
| 子任务1 搜索景点 | ✅ | Mock 数据 6 条 |
| 子任务2 生成 Word | ✅（修复后） | 修复前 500 |
| 子任务3 设置提醒 | ✅ | |
| 子任务4 发送邮件 | ✅ | 无密钥→`mock_sent`（见 H3） |
| 提交任务 | ✅ | |
| 问卷获取/提交 | ✅ | 内容不符需求（见模块4） |
| 管理员登录 | ✅ | |
| 监控面板 / 参与者分组 | ✅（修复后） | 修复前 500 |
| 导出 CSV | ✅ | 服务端 200 OK（自动化客户端读流异常，非服务端问题） |
| AI 助理对话(SSE) | ⚠️ 后端可用，前端阻断 | 后端校验 group/agent 正确（错 agent→400 正确）；前端面板对首次会话用户不显示 |

---

## 三、本次修复的崩溃 bug

1. **文档生成 500** — `document.py:28` `user.id[:8]`：`user.id` 是 UUID 对象不可切片 → 改为 `str(user.id)[:8]`。
2. **管理后台全 500** — `admin.py` 中 `User.group == ExperimentGroup.H` 触发 `character varying = experiment_group` 类型不匹配（DB 列是 VARCHAR，ORM 按原生枚举强转）。改为 `cast(User.group, String) == ExperimentGroup.H.value`，dashboard/participants/export 全部恢复。
3. **【发现未修复·高严重度】JWT 分组未在 consent 后刷新** — `userStore.consent()` 调 `/auth/consent` 后未刷新 JWT；`Task.tsx` 靠解码 `token.group` 决定显示 AI 面板。注册时签发的 token `group=None`，consent 后不更新 → **SOA/MOA 用户首次进入任务页永远看不到 AI 助理面板**（重新登录才正常，因 login 会带 group）。这直接破坏核心实验的 SOA/MOA 体验，需修复（建议 consent 后重新签发带 group 的 token）。

---

## 四、阻塞 / 待决项清单

| 类别 | 项 | 性质 | 建议优先级 |
|------|----|------|-----------|
| 修复 | JWT 分组刷新（consent 后重签 token） | 阻断核心实验 UI | 🔴 P0 |
| 修复 | 邮件失败静默成功（H3，mock_sent 伪装成功） | 数据真实性 | 🔴 P0 |
| 内容 | 问卷构念对齐需求（补"感知自主性""操纵检验"，去 PU/PEOU） | 研究效度 | 🟡 P1 |
| 实现 | 演示差异化（2.2）+ 确认机制/时长（2.3） | 缺失功能 | 🟡 P1 |
| 实现 | 管理员手动微调分组端点 | 缺失功能 | 🟡 P1 |
| 实现 | 异常报警（3×标准差）、OpenClaw 状态监控 | 缺失功能 | 🟢 P2 |
| 完善 | 状态机补"已完成"终态、导出加 Excel、单盲评审（JWT 含 group） | 完善项 | 🟢 P2 |

---

## 五、结论

- **代码骨架完整、分层合理**：6 大模块中约 70% 功能已实现，P0 主体（注册/分组/演示/任务/H组/问卷/采集/导出/后台）可跑通。
- **两处崩溃 bug 已修复并复测通过**（文档生成、管理后台）。
- **真正影响实验效度的不是"没写完"，而是几处准确性缺陷**：① 首次会话 AI 面板不可见；② 邮件失败被记为成功；③ 问卷构念与假设不符；④ 演示未差异化、无确认机制。这些恰好是"看起来能跑、但数据/体验不可信"的坑，建议优先修 P0 两项再做 P1 内容对齐。
