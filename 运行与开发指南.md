# 人机协作决策实验平台 — VS Code 使用・启动・使用指南

项目路径：`D:\27475\WorkBuddy-搭建网站\travel-experiment-platform`
技术栈：后端 FastAPI + SQLAlchemy + PostgreSQL；前端 React 18 + TypeScript + Vite + Zustand

---

## 一、用 VS Code 打开项目

1. 确认已安装 VS Code（https://code.visualstudio.com/）。
2. 打开方式（任选其一）：
   - **图形方式**：启动 VS Code → 菜单「文件」→「打开文件夹」→ 选中 `travel-experiment-platform` 文件夹 → 选择文件夹。
   - **命令行方式**（需安装时勾选了「添加到 PATH」）：在该文件夹右键「通过 Code 打开」，或在终端执行
     ```
     code "D:\27475\WorkBuddy-搭建网站\travel-experiment-platform"
     ```
3. 推荐安装扩展（左侧「扩展」图标，搜索名称安装）：
   - **Python**（Microsoft）—— 后端智能提示与调试
   - **ES7+ React Snippets** 或 **React Snippets** —— 前端组件快捷片段
   - **Tailwind CSS IntelliSense** —— 样式类名提示
   - **Prettier - Code formatter** —— 代码格式化
   - **Database Client**（可选）—— 直接在 VS Code 里看 PostgreSQL 数据
   - **REST Client**（可选）—— 用 `.http` 文件直接测后端接口

---

## 二、在 VS Code 里检查代码（代码结构地图）

打开后左侧「资源管理器」能看到两个核心子目录：`backend/`（后端）和 `frontend/`（前端）。

### 后端 `backend/app/`
| 路径 | 作用 |
|------|------|
| `main.py` | 程序入口。创建 FastAPI 应用，挂载所有路由（`/api/auth`、`/api/task`、`/api/agent`…），并注册 CORS |
| `routers/` | 各业务接口模块：`auth`（登录注册/同意）、`task`（任务/演示完成）、`email`（邮件）、`agent`（AI 助理）、`questionnaire`（问卷）、`admin`（管理后台）等 |
| `models/` | SQLAlchemy ORM 数据模型：`user.py`（参与者/分组）、`questionnaire.py`（问卷题项） |
| `schemas/` | Pydantic 请求/响应数据结构（接口入参出参的“契约”） |
| `services/` | 业务逻辑层：`seed_service.py`（初始数据播种）、`llm_client`（调用大模型，缺 key 时走 mock） |
| `database.py` | 数据库连接与初始化 |

> 读后端建议顺序：`main.py`（看路由挂载）→ `routers/auth.py`（看注册分组逻辑）→ `models/user.py`（看分组/状态字段）。

### 前端 `frontend/src/`
| 路径 | 作用 |
|------|------|
| `main.tsx` | React 入口，挂载 `<App/>` |
| `App.tsx` | **路由与权限守卫**。定义了参与者流程的页面跳转规则（见下方「使用流程」） |
| `pages/` | 每个界面对应一个文件：`Login` `Register` `Consent` `Demo` `Task` `Questionnaire` `Complete` |
| `pages/admin/` | 管理后台 7 个页面：`AdminDashboard`(看板) `AdminParticipants`(参与者) `AdminSubmissions`(提交与评分) `AdminQuestionnaire`(问卷配置) `AdminLogs`(行为日志) `AdminSettings`(系统配置) `AdminLogin` |
| `stores/` | Zustand 全局状态：`userStore.ts`（当前用户/令牌）、`chatStore.ts`（AI 对话）、`adminStore.ts` |
| `services/` | axios 封装的接口调用：`api.ts`（axios 实例）、`index.ts`（各接口函数） |
| `hooks/` | 自定义钩子：`useAgentChat`（AI 流式对话）、`useBehaviorLogger`（行为日志采集）、`useTimer`（计时） |
| `components/` | 复用组件：`ChatWindow`（对话窗口）、`ExecutionLog`（执行日志可视化） |

> 读前端建议顺序：`App.tsx`（理解流程）→ `stores/userStore.ts`（理解状态）→ `pages/Task.tsx`（核心任务页）→ `services/index.ts`（理解前后端怎么通信）。
> 快捷操作：按 `Ctrl+P` 输入文件名秒开；按住 `Ctrl` 点击函数/组件名可跳转定义；`Ctrl+Shift+F` 全局搜索。

### 配置文件（项目根目录）
- `docker-compose.yml`：整套 Docker 部署（PostgreSQL + 后端 + 前端），本地开发一般不用。
- `database/schema.sql` / `database/seed.sql`：数据库表结构和初始种子数据。
- `backend/.env`：后端配置（数据库地址、JWT 密钥、管理员账号 `admin/admin123`、各 API Key 留空则走 mock）。

---

## 三、启动后端

> 前提：PostgreSQL 已在运行（已确认本机服务 `postgresql-x64-17` 为 Running，5432 端口监听中），无需额外操作。

1. 在 VS Code 中打开**集成终端**（菜单「终端」→「新建终端」）。
2. 进入后端目录。推荐**直接用 venv 里的 python 以模块方式启动 uvicorn**，这样无需执行 `activate` 脚本，可绕开 PowerShell 对 `.ps1` 的执行策略限制（否则 `.\venv\Scripts\activate` 会被拦、且 `uvicorn` 会找不到）：
   ```
   cd D:\27475\WorkBuddy-搭建网站\travel-experiment-platform\backend
   .\venv\Scripts\python.exe -m uvicorn app.main:app --reload --port 8000
   ```
3. 验证启动成功：
   - 浏览器打开 `http://localhost:8000/api/health`，应返回 `{"status":"ok",...}`
   - 打开 `http://localhost:8000/docs` 可看到自动生成的交互式接口文档（可直接点开测试各接口）。

> 补充：若你确实想先激活虚拟环境（比如还要跑其他命令），在 PowerShell 里先执行一次
> `Set-ExecutionPolicy -Scope Process Bypass`，再 `.\venv\Scripts\activate` + `uvicorn app.main:app --reload --port 8000` 即可（该设置仅对当前终端本次会话有效）。

---

## 四、启动前端

> 注意：本机 `npm` 在 PowerShell 下会被执行策略拦截。最省事的办法是在 VS Code 里用**「命令提示符」终端**而不是 PowerShell。

1. 新建一个终端（保留后端那个终端继续运行）。
2. 进入前端目录并启动开发服务器。在 **PowerShell** 下 `npm` 会解析到被执行策略拦掉的 `npm.ps1`，请用下面任一种方式：
   - **最省事**：直接调用 `.cmd` 版本（批处理文件不受 PS 执行策略限制）：
     ```
     cd D:\27475\WorkBuddy-搭建网站\travel-experiment-platform\frontend
     npm.cmd run dev
     ```
   - 或先临时放开当前终端执行策略再正常 `npm`：
     ```
     Set-ExecutionPolicy -Scope Process Bypass
     npm run dev
     ```
   - 或干脆用**「命令提示符」(Command Prompt) 终端**（菜单「终端」→ 新建终端 → 下拉选 Command Prompt），那里 `npm run dev` 直接可用。
   - 若提示 `node_modules` 缺失，先执行一次 `npm install`（或 `npm.cmd install`）。
3. 启动成功后终端会显示 `Local: http://localhost:5173/`。
4. 前端通过 Vite 代理把 `/api` 请求转发到后端 `127.0.0.1:8000`，所以**前后端都要同时运行**页面才能正常调接口。

> 建议：开两个终端标签，一个跑后端、一个跑前端，互不干扰。

---

## 五、使用网站

### 参与者流程（普通用户）
1. 浏览器打开 `http://localhost:5173`。
2. **注册**：填邮箱 + 密码提交后，系统按 1:1:1 随机分配实验组（H 纯人工 / H+SOA 单 AI 助理 / H+MOA 三 AI 助理），且对参与者**单盲**（不告知分组）。
3. **知情同意**（Consent）：阅读并同意。
4. **观看演示**（Demo）：不同组的演示内容有差异。
5. **任务页**（Task）：完成「杭州 3 日游规划、预算 1000 元」四项子任务（景点搜索 / 生成 Word / 设置提醒 / 发送邮件）。SOA、MOA 组在此页可使用 AI 助理对话。
6. **问卷**（Questionnaire）：6 个构念共 15 题（含操纵检验题）。
7. **完成页**（Complete）。
> 页面跳转由 `App.tsx` 里的状态守卫控制，必须按上述顺序走，不能随意跳。

### 管理员流程
1. 打开 `http://localhost:5173/admin/login`。
2. 账号 `admin` / 密码 `admin123`（见 `backend/.env`）。
3. 后台可看：实验监控看板、参与者管理、任务提交与评分、问卷配置、行为日志、系统配置。

---

## 六、常见问题排查

- **后端启动报数据库连接失败**：检查 PostgreSQL 服务是否运行（`Get-Service postgresql*`），确认 `backend/.env` 中 `DATABASE_URL` 指向 `localhost:5432/travel_experiment`。
- **端口 8000 被占用**：用 `Get-NetTCPConnection -LocalPort 8000` 查占用进程并结束，或换 `--port 8001` 并同步改前端 `vite.config.ts` 的代理 target。
- **npm 报“无法加载 npm.ps1 / 禁止运行脚本”**：换「命令提示符」终端，或 PowerShell 里先 `Set-ExecutionPolicy -Scope Process Bypass`。
- **前端页面调接口 404/网络错误**：确认后端仍在运行，且前端用的是 `localhost:5173`（走代理），不要直接打开 8000。
- **想重置数据库**：停掉后端，用 `psql` 连 `travel_experiment` 重新执行 `database/schema.sql` 与 `database/seed.sql` 即可。

---

## 七、如何查看数据库数据

> 数据库：PostgreSQL，实验库名 `travel_experiment`，连接账号 `postgres` / `postgres`，端口 `5432`。
> 核心数据表：`users`（参与者/分组）、`task_submissions`（任务提交）、`questionnaire_responses`（问卷答案）、`questionnaire_items`（问卷题项）、`behavior_logs`（行为日志）、`reminders`（提醒）、`chat_messages`（AI 对话）、`admin_scores`（评分）、`system_config`（系统配置）、`admin_users`（管理员）。

### 方式一：pgAdmin 4 图形界面（最推荐，中文不乱码）

1. 打开：按 `Win` 键搜 **pgAdmin 4** 点击启动；或双击 `D:\\Develop\\PostgreSQL\\pgAdmin 4\\runtime\\pgAdmin4.exe`。
2. 首次会开一个浏览器网页，提示设主密码（随便设一个记下即可）。
3. 左侧树展开 **Servers → PostgreSQL 17**，弹窗密码填 `postgres`。
4. 再展开 **Databases → travel_experiment → Schemas → public → Tables**，即可看到所有表。
5. 右键某张表 → **View/Edit Data → All Rows**，右侧即出现数据表格，可直接浏览/编辑。

> 用 pgAdmin 查看中文**不会出现乱码**，这是它相对命令行的优势。日常查数据首选它。

### 方式二：VS Code 扩展（在编辑器内看）

1. 安装扩展 **Database Client**（或 **PostgreSQL** 类扩展，参见第一节推荐列表）。
2. 按 `Ctrl+Shift+P` → 连接 / New Query，连接参数填：
   - Host: `localhost`　Port: `5432`　User: `postgres`　Password: `postgres`　Database: `travel_experiment`
3. 写 SQL 选中 → 右键 **Run Query**，结果在下方面板。

### 方式三：命令行 psql（最快，但中文可能乱码）

```powershell
$env:PGPASSWORD="postgres"; $env:PGCLIENTENCODING="UTF8"
& "D:\Develop\PostgreSQL\bin\psql.exe" -h 127.0.0.1 -U postgres -d travel_experiment
```

进去后常用命令：
```sql
\dt                         -- 列出所有表
SELECT * FROM users;        -- 看参与者
\q                          -- 退出
```

> ⚠️ 即使设了 `PGCLIENTENCODING=UTF8`，PowerShell 控制台仍可能把中文显示成方块。想看干净中文，**优先用方式一 pgAdmin 4**。
> 另外 `group` 是 PostgreSQL 保留字，查 `users` 表的分组列必须加双引号：`SELECT "group" FROM users;`

### 常用查询（可直接粘到 pgAdmin 查询窗口）

```sql
-- 1. 看所有参与者及状态/分组
SELECT id, email, "group", status, demo_watch_seconds FROM users ORDER BY created_at DESC;

-- 2. 各状态人数分布
SELECT status, COUNT(*) FROM users GROUP BY status;

-- 3. 看某人的任务提交记录
SELECT * FROM task_submissions WHERE user_id = (SELECT id FROM users WHERE email='test01@test.com');

-- 4. 看问卷答案
SELECT * FROM questionnaire_responses LIMIT 20;

-- 5. 行为日志条数
SELECT COUNT(*) FROM behavior_logs;
```
