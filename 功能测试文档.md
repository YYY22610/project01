# 功能测试文档（手测用例）

cd D:\27475\WorkBuddy-搭建网站\travel-experiment-platform\backend
.\venv\Scripts\python.exe -m uvicorn app.main:app --reload --port 8000

cd D:\27475\WorkBuddy-搭建网站\travel-experiment-platform\frontend
npm.cmd run dev




适用环境：前后端均已启动
- 后端：`http://localhost:8000`（已确认 `/api/health` 返回 200）
- 前端：`http://localhost:5173`

> 判断标准：每一条「预期结果」全部命中 = 通过（✓）；任一项不符 = 失败（✗），把不符现象记到「实际结果」栏并反馈。

---

## 0. 如何启动前后端

> 前置条件：已安装 Python 虚拟环境（项目内 `backend/venv`）、Node 依赖（前端 `node_modules` 已装）、且 **PostgreSQL 服务正在运行**（本机服务名 `postgresql-x64-17`，默认自启）。

建议：在 VS Code 里开**两个终端标签**，一个跑后端、一个跑前端，互不干扰。

### 0.1 启动后端（FastAPI，端口 8000）

在 VS Code 集成终端执行：

```powershell
cd D:\27475\WorkBuddy-搭建网站\travel-experiment-platform\backend
.\venv\Scripts\python.exe -m uvicorn app.main:app --reload --port 8000
```

> 说明：不要执行 `.\venv\Scripts\activate` 再 `uvicorn`，因为 `activate` 是 `.ps1` 脚本，会被 PowerShell 执行策略拦截；直接调用 venv 内的 `python.exe -m uvicorn` 可绕开该限制。若确实想先 activate，可先执行一次 `Set-ExecutionPolicy -Scope Process Bypass`。

**启动成功标志**（终端出现）：
```
Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
INFO:     Application startup complete.
```

**验证**：浏览器打开 `http://localhost:8000/api/health`，应返回 `{"status":"ok",...}`。
接口文档见 `http://localhost:8000/docs`。

### 0.2 启动前端（React + Vite，端口 5173）

在**另一个终端**执行（PowerShell 下用 `npm.cmd` 绕开执行策略）：

```powershell
cd D:\27475\WorkBuddy-搭建网站\travel-experiment-platform\frontend
npm.cmd run dev
```

> 说明：PowerShell 里直接 `npm` 会解析到被拦截的 `npm.ps1`，改用 `npm.cmd` 即可；或在终端先 `Set-ExecutionPolicy -Scope Process Bypass` 再 `npm run dev`；也可改用「命令提示符」(Command Prompt) 终端直接 `npm run dev`。若提示 `node_modules` 缺失，先跑一次 `npm.cmd install`。

**启动成功标志**（终端出现）：
```
VITE ready in xxx ms
  ➜  Local:   http://localhost:5173/
```

**验证**：浏览器打开 `http://localhost:5173`，应显示登录/注册页，无白屏。

### 0.3 关键注意

- 前端通过 Vite 代理把 `/api` 请求转发到后端 `127.0.0.1:8000`，因此**前后端必须同时运行**，页面才能正常调接口。
- 后端依赖 PostgreSQL，若启动报数据库连接失败，先确认 PostgreSQL 服务在跑（`Get-Service postgresql*`）。
- 8000 端口被占用时，用 `Get-NetTCPConnection -LocalPort 8000` 查占用进程并结束，或换 `--port 8001`（同步改前端 `vite.config.ts` 代理 target）。

---

## 1. 测试前准备（必做）

| 检查项 | 操作 | 预期 |
|--------|------|------|
| 后端存活 | 浏览器开 `http://localhost:8000/api/health` | 返回 `{"status":"ok",...}` |
| 前端存活 | 浏览器开 `http://localhost:5173` | 显示登录/注册页，无白屏 |
| 两个终端在跑 | 看 VS Code 终端 | 后端有 `Uvicorn running`，前端有 `Local: http://localhost:5173/` |

> 注意：当前 `.env` 中 `LLM_API_KEY`、`RESEND_API_KEY`、`BING_SEARCH_API_KEY` 均为空，
> 因此 **AI 助理走 mock 回复、邮件发送必然失败（这是预期行为，不是 bug）**。详见第 4 节。

---

## 1. 参与者端测试（核心实验流程）

流程顺序由 `App.tsx` 守卫控制，必须按序走：注册 → 知情同意 → 演示 → 任务 → 问卷 → 完成。

### TC-01 注册与随机分组
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1.1 | 打开 `http://localhost:5173`，点「注册」 | 进入注册表单 |
| 1.2 | 填邮箱（如 `test1@test.com`）+ 密码，提交 | 自动跳转（进入知情同意或登录页） |
| 1.3 | 用管理员后台查该用户分组 | 数据库 `users.group` 已被赋值为 `H` / `H+SOA` / `H+MOA` 之一（1:1:1 随机） |
| 1.4 | 重复注册 2~3 个不同邮箱 | 能看到至少两种不同分组（验证随机分配生效） |

> 验证分组最方便的方式：登录管理员后台（第 3 节）→ 参与者管理，看该用户的「分组」列。

### TC-02 知情同意（Consent）
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 2.1 | 注册后自动进入同意页 | 显示实验说明/知情同意文本 |
| 2.2 | 勾选同意并提交 | 跳转至演示页（Demo），用户状态变为可进入下一步 |

### TC-03 演示页（Demo，分组差异化）
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 3.1 | 进入演示页 | 显示演示内容，且**不同分组看到的文案/模块有差异** |
| 3.2 | 勾选「我已理解」类确认项（如有）并提交 | 跳转至任务页（Task） |

### TC-04 任务页（Task，四项子任务）
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 4.1 | 进入任务页 | 显示「杭州 3 日游规划、预算 1000 元」任务说明与子任务区 |
| 4.2 | 景点搜索子任务 | 能触发搜索（mock 模式下返回示例结果，不报错） |
| 4.3 | 生成 Word 子任务 | 能生成并下载/预览 .docx 文件 |
| 4.4 | 设置提醒子任务 | 能提交提醒，前端提示成功 |
| 4.5 | 发送邮件子任务 | 点击发送 → **预期弹「发送失败」提示，且页面不标记为「已发送」**（因无邮件 API Key，属预期） |
| 4.6 | 完成任务、进入下一步 | 状态流转到可填问卷 |

### TC-05 问卷（Questionnaire，6 构念 15 题）
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 5.1 | 进入问卷页 | 显示 6 个构念分组、共 15 道题 |
| 5.2 | 含选择题（操纵检验题） | 选择题以按钮/选项形式渲染，可选中 |
| 5.3 | 量表题 | 可打分/选择 |
| 5.4 | 提交问卷 | 跳转至完成页，状态变为 `questionnaire_completed` |

### TC-06 完成页（Complete）
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 6.1 | 进入完成页 | 显示「实验完成」感谢信息 |
| 6.2 | 刷新页面 | 仍停在完成页（状态已持久化，不会回退） |

---

## 2. AI 助理测试（SOA / MOA 组才出现）

> 仅当该测试账号被分到 `H+SOA` 或 `H+MOA` 时，任务页才显示 AI 对话入口。若分到 `H` 组则无此模块，属正常。

### TC-07 SOA 组（单 AI 助理）
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 7.1 | 用 SOA 组账号进任务页 | 出现 1 个 AI 对话窗口 |
| 7.2 | 输入「帮我规划第一天行程」 | 返回 mock 回复（无 API Key 时），不报错、不卡死 |
| 7.3 | 观察执行日志 | 对话/工具调用过程有可视化日志（ExecutionLog） |

### TC-08 MOA 组（三 AI 助理）
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 8.1 | 用 MOA 组账号进任务页 | 出现 3 个独立助理入口（AgentA/B/C） |
| 8.2 | 用 `@助理名` 指定下达指令 | 指令路由到对应助理，返回对应回复 |
| 8.3 | 三个助理相互独立 | 各自对话历史分开，不串台 |

---

## 3. 管理员端测试

地址：`http://localhost:5173/admin/login`，账号 `admin` / 密码 `admin123`（见 `backend/.env`）。

### TC-09 管理员登录
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 9.1 | 打开 `/admin/login`，输入 admin/admin123 | 登录成功，进入看板 |
| 9.2 | 输错密码 | 提示登录失败，不进入后台 |

### TC-10 实验监控看板（Dashboard）
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 10.1 | 进入看板 | 显示参与者总数、各分组人数、任务完成进度等统计 |
| 10.2 | 对照你刚注册的测试账号 | 看板计数随新注册/完成实时变化 |

### TC-11 参与者管理（Participants）
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 11.1 | 打开参与者列表 | 能看到所有注册用户及其**分组、状态、注册时间** |
| 11.2 | 找到 TC-01 注册的测试账号 | 分组与第 1.3 步记录一致 |
| 11.3 | 查看行为/进度详情 | 能展开看到该用户的任务进度 |

### TC-12 任务提交与评分（Submissions）
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 12.1 | 打开提交与评分 | 列出参与者提交的任务内容 |
| 12.2 | 对某条提交录入评分（1~10） | 评分可保存，列表显示评分值 |

### TC-13 问卷配置（Questionnaire）
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 13.1 | 打开问卷配置 | 显示 6 构念、15 题及选项（含 2 道选择题的 options） |
| 13.2 | 配置项与前端问卷一致 | 构念标签、题数、题型均匹配 |

### TC-14 行为日志（Logs）
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 14.1 | 打开行为日志 | 显示参与者操作流水（如 page_view、按钮点击等） |
| 14.2 | 回到前端操作几步再刷新日志 | 能看到刚产生的新的行为记录 |

### TC-15 系统配置（Settings）
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 15.1 | 打开系统配置 | 显示系统参数（如实验名称、样本量配置等） |
| 15.2 | 修改并保存（如有可编辑项） | 保存成功提示，刷新后仍生效 |

---

## 4. 边界与已知行为验证（重要，避免误判）

### TC-16 邮件失败守卫（预期失败 = 功能正常）
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 16.1 | 任务页点「发送邮件」 | 前端弹「发送失败」提示 |
| 16.2 | 检查页面状态 | **不应**标记为「已发送」（守卫生效，不静默成功） |
| 16.3 | 看后端终端日志 | 出现邮件发送异常 / 502 相关记录 |

> 说明：因 `RESEND_API_KEY` 为空，邮件必然失败。这里要测的是「失败能被正确暴露、不伪装成功」，这正是之前修复的 P0 项。

### TC-17 JWT 分组信息（刷新后保持）
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 17.1 | 注册后拿到 token | token 内含该用户分组信息 |
| 17.2 | 知情同意后 token 刷新 | 新 token 仍携带正确分组，AI 面板能正确显示分组 |

### TC-18 无 API Key 时的降级
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 18.1 | AI 对话（SOA/MOA 组） | 返回 mock 回复，界面不崩溃 |
| 18.2 | 景点搜索 | 返回示例/模拟结果，不报错 |

---

## 5. 后端接口自测（可选，用 `/docs`）

打开 `http://localhost:8000/docs`，可手动点开以下接口验证：
- `POST /api/auth/register` — 注册
- `POST /api/auth/login` — 登录拿 token
- `GET /api/auth/me` — 查当前用户（含分组/状态）
- `POST /api/task/demo-complete` — 演示完成
- `POST /api/email/send` — 发送邮件（预期 502，验证守卫）
- `GET /api/questionnaire/items` — 问卷题项（应返回 15 题、6 构念）
- `GET /api/admin/participants` — 参与者列表（需 admin token）

---

## 6. 测试结果记录表（自己填）

| 用例 | 描述 | 结果(✓/✗) | 实际现象/备注 |
|------|------|-----------|----------------|
| TC-01 | 注册与随机分组 | | |
| TC-02 | 知情同意 | | |
| TC-03 | 演示差异化 | | |
| TC-04 | 任务四项子任务 | | |
| TC-05 | 问卷 6 构念 15 题 | | |
| TC-06 | 完成页持久化 | | |
| TC-07 | SOA 单助理 | | |
| TC-08 | MOA 三助理 | | |
| TC-09 | 管理员登录 | | |
| TC-10 | 看板统计 | | |
| TC-11 | 参与者管理 | | |
| TC-12 | 提交与评分 | | |
| TC-13 | 问卷配置 | | |
| TC-14 | 行为日志 | | |
| TC-15 | 系统配置 | | |
| TC-16 | 邮件失败守卫 | | |
| TC-17 | JWT 分组 | | |
| TC-18 | 无 Key 降级 | | |
