# 功能差距分析：对照《7_21实验网站设计.docx》

> 分析对象：travel-experiment-platform（前端 React+TS+Vite，后端 FastAPI+SQLAlchemy+PostgreSQL）
> 对照文档：研究二「人机协作决策实验平台 — 功能需求文档」（2026-07-21 版，241 行）
> 分析方法：提取设计文档全文 + 逐一核对后端路由/模型、前端页面/组件/钩子实际代码
> 生成日期：2026-07-23

---

## 一、总体结论

平台已搭起完整骨架，P0/P1 主线（注册分组 → 演示 → 任务四子任务 → 问卷 → 完成、管理后台基础功能）**已跑通**。但对照 7_21 设计文档，**仍有若干明确未实现项与多处"部分实现/缝隙"**，集中在：

- 人口统计学采集（前端缺入口）
- 问卷按组替换（H 组仍看"助理"题）
- 评分 8 因子 100 分制（当前仅两个 1-10 总分）
- 管理后台监控数据（异常报警 / API 状态后端未供数）
- 日志字段完整度（远少于设计"七"要求）
- 任务草稿自动保存（刷新即丢）
- 管理员手动微调分组（此前搁置）

标记说明：【已实现】 / 【部分实现】 / 【未实现】

---

## 二、逐模块差距明细

### 模块1：用户注册与随机分组系统

| 需求 | 状态 | 说明 |
|------|------|------|
| 1.1 注册采集人口统计学（年龄/性别/教育/计算机频率/AI经验） | 【未实现】 | 后端 `User` 模型含 `age/gender/education/tech_frequency/ai_experience` 字段，且有 `/api/auth/demographics` 端点；但**前端 `Register.tsx` 仅收集 email+password，没有人口统计填写步骤/页面**，数据无从录入。 |
| 1.2 自动随机分组 1:1:1 | 【已实现】 | `group_service.assign_group` 按 1:1:1 分配，dashboard 显示均衡。 |
| 1.2 管理员手动微调分组 + 数据展示筛选 | 【未实现】 | 后端无手动改 `group` 的端点，前端无入口（此前需求确认"暂搁置"）。 |
| 1.2 分组不可见（单盲） | 【已实现】 | `/api/auth/me` 不返回 `group`；前端仅解码 JWT 判断"是否显示 AI 面板"，不向用户展示组名。 |
| 1.3 状态机（6 阶段、不可回退） | 【已实现】 | `state_machine.VALID_TRANSITIONS` 严格单向：`registered→consented→demo_completed→task_in_progress→task_completed→questionnaire_completed`，无回退路径。 |

### 模块2：任务说明与演示模块

| 需求 | 状态 | 说明 |
|------|------|------|
| 2.1 统一任务指令常驻展示（N日/M元/四项子任务） | 【部分实现】 | 任务页 header 仅显示标题"旅行规划任务"与计时器，**未常驻展示具体任务指令文本**（杭州N日游、预算M元、四项子任务说明）。N/M 可在后台配置（`task_days`/`task_budget`），但**前端任务页多处硬编码"杭州三日游"**，配置未真正联动。 |
| 2.2 分组差异化演示 | 【已实现】 | `Demo.tsx` 已按 H/SOA/MOA 展示差异化内容（此前修复）。 |
| 2.3 演示确认 + 观看时长 | 【已实现】 | "我已理解"按钮 + `demo_watch_seconds` 落库。 |

### 模块3：任务执行界面（核心模块）

| 需求 | 状态 | 说明 |
|------|------|------|
| 3.1 通用组件（任务面板/Checklist/计时器/提交区） | 【已实现】 | `Task.tsx` 含常驻 Checklist、计时器（`useTimer`）、提交按钮（校验四项全完成后可提交）。 |
| 3.2 H组工具（搜索/富文本编辑/日历/邮箱） | 【部分实现】 | 功能基本覆盖但为简化版：搜索=简单输入框调后端；文档=textarea + 后端生成 Word（**非设计要求的 OnlyOffice/富文本编辑器**）；提醒=datetime-local；邮箱=按钮发送。 |
| 3.3 SOA界面（对话/执行日志/结果预览编辑/干预） | 【部分实现】 | `ChatWindow` + `ExecutionLog` 已实现对话与日志可视化；但"助理产出可预览、参与者修改确认后再提交"的结果预览编辑闭环、以及"随时中断重新下达指令"的干预控件未明显体现。 |
| 3.4 MOA界面（多助理/分工/@指定/结果整合/WebSocket） | 【部分实现】 | 已实现 `moa_a/b/c` 三实例面板与独立日志、`@指定`（前端解析 `agent_id` 路由到对应端点）；"分工可视化""结果整合视图"为简化呈现；设计要求的 **WebSocket 改为 SSE**（功能等价，非缺失）；未对接真实 OpenClaw API（当前 MockLLM）。 |

### 模块4：任务后评价问卷

| 需求 | 状态 | 说明 |
|------|------|------|
| 6 构念 15 题（含操纵检验） | 【已实现】 | questionnaire 播种 6 构念 15 题（Trust/Autonomy/Satisfaction/TaskLoad/FutureUse/ManipulationCheck）。 |
| H组与"助理"相关问题替换为"工具"评价或跳过 | 【未实现】 | 当前所有组（含 H 组）渲染同一套问卷，H 组用户也会看到"对助理的信任"等题项，不符合单盲设计与文档明确要求。 |

### 模块5：数据采集与客观评分

| 需求 | 状态 | 说明 |
|------|------|------|
| 5.1 客观指标采集 | 【部分实现】 | 任务完成度（task1-4 布尔）✅、Word 生成与路径✅、邮件状态✅、任务用时(`duration_ms`)✅；但"提醒正确性比对""行为日志 JSON"较粗。 |
| 5.2 评分录入（行程合理性 + 综合质量） | 【部分实现】 | `AdminSubmissions` 可录 `rationality_score` + `quality_score`（1-10）。 |
| 五、客观评分指标（100分制 8 因子逐项打分） | 【未实现】 | 设计明确要求按 8 项因子（观赏游憩30/历史文化25/珍稀15/规模10/完整性5/知名度10/适游期5/环保-5~3）逐项打分汇总 0-100。**当前仅存两个 1-10 总分，模型(`AdminScore`)与前端录入均无 8 因子字段。** |
| 5.3 数据导出 CSV/Excel + 按组别筛选 | 【部分实现】 | 后端有 `/export/participants`、`/export/logs`、`/export/scores`（CSV）；**无 Excel、无按组别筛选参数**。AdminSettings 的"导出全部数据"按钮调用 `/api/admin/export/all`，**后端无此端点 → 按钮失效**。 |

### 模块6：管理后台

| 需求 | 状态 | 说明 |
|------|------|------|
| 6.1 监控面板（各组样本量/整体进度/异常报警） | 【部分实现】 | 样本量✅；但后端 `/admin/dashboard` **未返回 `completion_rate` / `abnormal_count`（超3倍标准差）/ `recent_registrations`**，前端对应卡片（完成率、异常数、最近注册）始终显示默认值/0，即**异常报警与进度监控为空壳**。 |
| 6.2 OpenClaw 服务状态监控（API成功率/响应时间/暂停切换） | 【未实现】 | 后端无相关端点与数据采集；前端 API 状态卡片显示 unknown，无"服务中断暂停实验"开关。 |
| 6.3 基础配置（任务参数N/M/邮箱动态调整、启用停用、导出原始数据） | 【部分实现】 | 配置编辑 UI 齐全（`experiment_status`/`target_sample_size`/`task_days`/`task_budget`/`recipient_email` 等）；启用停用✅；但任务页未联动配置（硬编码三日游），"导出全部原始数据"按钮失效（见 5.3）。 |

### 关键注意事项

| 需求 | 状态 | 说明 |
|------|------|------|
| 分组不可见 | 【已实现】 | — |
| 任务标准化（三组指令一致） | 【已实现】 | 指令文本固定，仅工具/助理数量不同 |
| 助理失败容错（明确错误/重试/手动干预） | 【部分实现】 | 邮件失败有提示并禁止误标成功（此前修复）；搜索/文档失败仅 alert，无显式重试/干预 UI |
| 数据完整性（刷新/断网保留进度草稿） | 【未实现】 | 任务进度（搜索结果/文档内容/提醒/邮件）存于前端 state，**刷新即丢失**；仅行为日志有 localStorage 兜底，任务草稿无自动保存 |
| 实验伦理（知情同意/可随时退出） | 【部分实现】 | 有 Consent 同意书页；"参与者可随时退出"的退出机制未实现 |

---

## 三、日志字段覆盖差距（设计"六、七"）

当前 `behavior_logs` 模型字段：`user_id, group, timestamp, action_type, action_target, input_content, ai_response, agent_id, page_path, session_id, extra_data(JSONB)`。前端 `useBehaviorLogger` 实际仅写入 `action_type/action_target/input_content/page_path`。

设计"七、完整日志字段总表"要求的大量字段**基本未采集**：

- **会话元数据**：`session_start/end_time`、`user_agent`、`screen_resolution`、`experiment_version` 等缺失；
- **信息检索行为**：`search_count`、`results_viewed`、`result_view_duration`、`clicked_item_id/timestamp` 等缺失；
- **AI 交互行为**：`ai_suggestion_id`、`ai_suggestion_type`、`ai_response_latency`、`user_action_on_ai`、`modification_details`、`ai_interaction_rounds`、`ai_suggestion_click_sequence` 等缺失；
- **决策过程指标**：`clickstream_sequence`、`phase_durations`、`manual_edit_count`、`final_plan_text`、`final_plan_submit_timestamp` 等缺失。

> `extra_data` 是 JSONB，理论上可扩展，但需要前后端共同补齐采集逻辑。

---

## 四、关键技术差异（非功能缺失）

- **WebSocket → SSE**：设计 3.4 要求 WebSocket 实时日志；项目用 SSE 流式推送，功能等价，属技术选型差异。
- **真实 OpenClaw/LLM 对接**：当前无 API Key 走 `MockLLMClient`，Agent 框架（`base_agent`/`agent_factory`/`tools`）已搭好，接入真实 Key 即可。这是**部署前提**，非代码缺失。
- **真实搜索/邮件 API**：同理，配置 Key 后启用；无 Key 时搜索/邮件相关子任务会失败（符合此前修复的"失败守卫"预期）。

---

## 五、下一步开发优先级建议

### 【P0 必修】影响实验有效性与数据完整性
1. 前端补充人口统计学采集页（注册/同意后立即填写）→ 打通 `/demographics`。
2. 问卷按组替换：H 组将"助理"相关题替换为"工具"评价或跳过。
3. 任务草稿自动保存（刷新/断网不丢进度）—— 否则数据完整性不达标。

### 【P1 重要】评分与研究价值
4. 评分模块改造为 8 因子 × 100 分制（改 `AdminScore` 模型 + 前端录入 + 汇总）。
5. 后端 `/admin/dashboard` 补齐 `completion_rate` / `abnormal_count` / `recent_registrations` 数据，让监控面板与异常报警真正生效。
6. 修复 AdminSettings「导出全部数据」失效（补 `/export/all` 或改为调用现有三个导出）。

### 【P2 增强】
7. 管理后台手动微调分组端点 + 前端入口。
8. OpenClaw 服务状态监控（成功率/响应时间/暂停开关）。
9. 日志字段按设计"七"补齐（尤其 AI 交互细粒度与决策过程指标）。
10. 任务指令常驻展示 + N/M 配置联动到任务页文案。
