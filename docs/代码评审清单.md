# 代码评审清单（PR 必过项）

> 每次开 PR 前，作者先 self-review 逐项勾选；评审人复核。
> 🔴 = 阻断（不修不能合）　🟡 = 建议（应修，除非有理由）
> 每条标注对应"架构铁律"编号与历史事故出处，便于教学。

---

## A. 安全 & 实验效度　🔴

- [ ] **A1** 参与者相关代码是否泄露分组？JWT 是否含 `group`？前端是否 `atob` 解码 `group`？（铁律1 / auth_service.py、Task.tsx）
- [ ] **A2** 有无新增硬编码密钥 / 密码 / API Key 或可被直接用于生产的默认值？（铁律2 / config.py）
- [ ] **A3** 文件下载 / 上传路径是否做 `os.path.basename` + 目录归属校验？（铁律6 / document.py）
- [ ] **A4** 管理类接口是否校验 `role`？（铁律8 / deps.py `get_current_admin`）

## B. 正确性 & 数据真实　🔴

- [ ] **B1** 影响业务判定的异常是否被静默吞掉或伪装成功？（铁律3 / email.py 事故）
- [ ] **B2** 新增 / 修改的"完成状态"是否以后端为唯一真相源，而非前端本地 state？（铁律4 / Task.tsx）
- [ ] **B3** AI（Agent）完成的子任务是否回写 `TaskSubmission` 对应 `taskN_*` 字段？（铁律5 / agent_tools.py）
- [ ] **B4** 切换 Mock / 真实依赖时，返回结构（如 `tool_calls`）是否归一化为一致形态？（铁律7 / llm_client.py、base_agent.py）

## C. 并发 & 事务　🔴/🟡

- [ ] **C1** 涉及计数 / 分配的写操作是否加锁或事务隔离，避免并发竞态？（分组分配 H6 / group_service.py）

## D. 代码质量　🟡

- [ ] **D1** 有无 3 行以上重复逻辑应抽公共函数？（submission 查找重复 4 处 / document、reminder、email、task）
- [ ] **D2** 是否引入 `any` 或缺类型声明？（前端 API 响应须 `interface`）
- [ ] **D3** 列表 `key` 是否用稳定 id 而非数组下标 `key={i}`？
- [ ] **D4** 有无死代码 / 声明未使用的变量？（如 Task.tsx 的 `hasAI`）
- [ ] **D5** `catch` 是否使用统一错误 UI，而非原生 `alert()`？（仅 Register 已改善）
- [ ] **D6** 同一事件名是否在不同位置被赋予不同语义？（如 `search_result_click` 混用）

## E. 测试 & 文档　🟡

- [ ] **E1** 功能改动是否带测试？（必含：分组并发、状态机非法流转、tool_calls 解析）
- [ ] **E2** 关键 / 反直觉逻辑是否有注释说明"为什么"（而非"做了什么"）？

---

## 使用约定

- 任一 🔴 项未过 → PR 阻塞，必须修
- 🟡 项未过 → 需在 PR 说明中给出不修的理由，评审人确认
- 清单随事故持续演进：每发现一类新事故，提炼为一条铁律并挂到对应检查项
