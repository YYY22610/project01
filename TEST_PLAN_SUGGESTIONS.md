# 引导词（Suggestion Chips）功能测试方案

> 目标：验证对话界面引导词在「开场 / 推进 / 收尾」三阶段正确切换，且每次点击都能推进旅游攻略流程直至生成 Word。
> 适用范围：`ChatWindow.tsx` 的 `SUGGESTION_SETS`，覆盖 soa / moa_a / moa_b 三个助理。
> 文档版本：v2 —— 已包含「推进阶段自动追加收尾组」行为（2026-08-03 更新）。

---

## 一、阶段触发规则（判定基准）

| 当前状态 | 显示阶段 | 引导词来源 | 视觉样式 |
|---------|---------|-----------|---------|
| 对话为空（无任何消息） | `start` 开场 | 仅显示"开场"组 | 浅蓝 pill |
| 已聊天，但用户**未**说过导出类关键词 | `progress` 推进 | 显示"推进"组 **+ 自动追加"收尾"组** | 推进词浅蓝；收尾词绿色(emerald) + 分隔提示 |
| 用户消息含 `生成|导出|word|保存攻略|发我|文档` 任一词 | `done` 收尾 | 仅显示"收尾"组 | 绿色(emerald) pill |

**关键变化（v2）**：在 `progress` 阶段，"收尾"组引导词（生成 Word / 发邮件等）**直接常驻显示**，无需用户先手输"生成"即可点击触发导出。绿色样式用于区分"推进"与"收尾/导出"动作。

点击气泡 = 发送该文本（等价于手动输入并回车），会写入行为日志 `agent_message`。

---

## 二、前置准备

1. 启动后端：`backend` 下 `uvicorn app.main:app --reload --port 8000`（用真实 LLM 需 `.env` 已填 Qwen Key；mock 模式 `LLM_API_KEY=""` 亦可跑通流程）。
2. 启动前端：`frontend` 下 `npm run dev`，浏览器打开任务页。
3. 用 **SOA 组** 测试账号登录（单 AI 组），进入任务对话页。
4. 打开浏览器 F12 → Network / Console，便于核对请求与行为日志。

---

## 三、主流程测试用例（SOA 完整攻略）

用户"小张"目标：做一份休闲、预算 1000 内的杭州 3 日游攻略并导出 Word。

| 步骤 | 用户动作 | 触发/显示的引导词 | 预期 AI 行为 | 检查点 |
|------|---------|------------------|-------------|--------|
| S1 | 进入 SOA 对话（空态） | 仅显示**开场**4 条（浅蓝）：休闲3日游 / 预算紧 / 带老人小孩 / 爱自然美食 | 空态提示"在下方输入框发送消息" | 气泡出现在输入框上方，浅蓝 pill |
| S2 | 点击「我想安排休闲一点的 3 日游」 | 切换为**推进**组 + **收尾**组（绿色，含分隔提示"以下为收尾操作…"） | 调 `search_attractions`，状态面板"正在搜索景点…"，返回结构化草案+反问 | 气泡文本被发送；行为日志新增 `agent_message`；阶段变 progress；此时已可见绿色"生成 Word"气泡但**未自动触发** |
| S3 | 点击「我预算紧，控制在 1000 元以内」 | 仍显示推进 + 收尾（绿色） | 结合历史调整方案，强调免费/低价景点 | 多轮历史生效（LLM 记得 S2 偏好） |
| S4 | 点击「帮我算一下目前总预算」 | 仍显示推进 + 收尾（绿色） | 调 `calculate_budget`，返回预算明细 | 状态面板显示"正在计算预算…" |
| S5 | 点击「多推荐几个西湖周边免费景点」 | 仍显示推进 + 收尾（绿色） | 再次调 `search_attractions` 补足免费点 | 不触发 Word 生成（未点击收尾词） |
| S6 | **直接点击绿色「就按这个生成 Word 攻略」**（无需手输"生成"） | 命中导出关键词 → 阶段切 `done`，仅显示**收尾**组 | 调 `generate_docx`，状态面板"正在生成行程文档…"，返回 .docx 下载链接 | 绿色收尾气泡点击即触发导出；下载链接可点击 |
| S7 | 点击「把攻略发到我邮箱」 | 仍显示**收尾**组 | 调 `send_email`（需真实 SMTP/授权码） | 邮件发送成功提示 |

**关键断言**：
- 推进阶段（S2–S5）`generate_docx` **不会自动触发**；它只在用户**主动点击绿色收尾气泡**或手输导出词（S6）后才调用。
- 收尾组在 `progress` 阶段即常驻可见（v2 核心行为），降低用户"不知道怎么导出"的操作门槛。

---

## 四、阶段切换边界用例

| 用例 | 操作 | 预期引导词阶段 | 说明 |
|------|------|---------------|------|
| B1 | 清空对话/新会话，不发任何消息 | `start` 开场（仅开场组） | 空态判定 |
| B2 | 发一条普通问句（如"杭州天气如何"） | `progress` 推进（推进组 + 绿色收尾组） | 无导出关键词，但收尾词已可见 |
| B3 | 发含"导出"的消息（如"先不导出"） | `done` 收尾（仅收尾组） | 正则命中即切收尾（注意：即使语义是否定，关键词仍触发；如需更精确可改正则） |

> 备注：B3 揭示当前正则较粗——"先不导出"也会误切收尾。若实验需严谨，建议改为白名单触发（如"生成攻略/导出Word/保存文档"）。

---

## 五、MOA 分组用例（职责边界）

| 用例 | 操作 | 预期 |
|------|------|------|
| M1 | 切到 moa_a（检索专员），点击「帮我搜杭州经典必游景点」 | 仅调 `search_attractions`；**不会**调 `generate_docx`（职责限定）；收尾组气泡虽显示但 moa_a 收尾词为"信息够了，交给编排专员出方案"等非导出项 |
| M2 | 切到 moa_b（编排专员），点击「根据已有景点帮我排 3 天行程」 | 调 `generate_docx` / `calculate_budget`；显示编排组引导词（推进 + 绿色收尾） |

> 说明：所有三个助理（soa/moa_a/moa_b）在 `progress` 阶段都会显示"推进 + 绿色收尾"两组气泡，区别仅在于各自 `SUGGESTION_SETS` 的文案不同。

---

## 六、交互细节检查

- [ ] 流式响应中（`isStreaming`），引导词气泡 `disabled`，呈灰色不可点。
- [ ] 点击气泡后，气泡随阶段自动更新（无需刷新）。
- [ ] **`progress` 阶段可见浅蓝"推进"气泡与绿色"收尾"气泡并存，且有分隔提示文字**。
- [ ] 绿色收尾气泡点击后直接触发 `generate_docx`（无需先手输关键词切阶段）。
- [ ] 引导词文案与 `SUGGESTION_SETS` 完全一致（详见代码）。
- [ ] 点击气泡产生的行为日志 `action_type=agent_message`、`agent_id` 正确（soa/moa_a/moa_b）。

---

## 七、验收标准

1. 三阶段引导词在 SOA 下按 S1→S7 顺序正确切换。
2. `generate_docx` 仅在用户**主动点击绿色收尾气泡**或手输导出词后触发，不会在 `progress` 阶段自动生成。
3. `progress` 阶段收尾组（绿色）常驻可见，用户可一键直达导出，无需先手输"生成"。
4. MOA 两个专员按职责边界调用工具，不串岗。
5. 流式中气泡禁用、点击即发送、行为日志完整。
